export default class Edge {
  constructor(weight, sourceVertex, destinationVertex) {
    this.weight = weight;
    this.sourceVertex = sourceVertex;
    this.destinationVertex = destinationVertex;
  }
}
